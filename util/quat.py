# NOTE: Scipy has native(ish) support for quaternions,
# so depends on what you want to use. Other alternatives:
# https://github.com/KieranWynn/pyquaternion (https://pypi.org/project/pyquaternion/)
# https://pypi.org/project/numpy-quaternion/ -> NOTE: I have just realised that this pulls in SciPy anyway,
# so we can just use SciPy instead
